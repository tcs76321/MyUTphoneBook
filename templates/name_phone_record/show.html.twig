{% extends 'base.html.twig' %}

{% block title %}NamePhoneRecord{% endblock %}

{% block body %}
    <h1>NamePhoneRecord</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ name_phone_record.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ name_phone_record.name }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ name_phone_record.phoneNumber }}</td>
            </tr>
            <tr>
                <th>Favorited</th>
                <td>{{ name_phone_record.favorited ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ name_phone_record.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('name_phone_record_index') }}">back to list</a>

    <a href="{{ path('name_phone_record_edit', {'id': name_phone_record.id}) }}">edit</a>

    {{ include('name_phone_record/_delete_form.html.twig') }}
{% endblock %}
